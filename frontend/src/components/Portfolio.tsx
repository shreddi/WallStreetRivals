import React from 'react'
import { Button, Flex, Box, Text, TextInput, ActionIcon, Pagination,} from '@mantine/core';

const Portfolio: React.FC = () => {
    return(
        <Text>
            hello
        </Text>
    )
}

export default Portfolio